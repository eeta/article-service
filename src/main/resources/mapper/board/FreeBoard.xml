<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eeta.webzine.board.free.dao.FreeBoardDao">

  <select id="getFreeBoardList" resultType="com.eeta.webzine.board.free.dto.FreeBoardPostDto">
    select
      post_id as postId,
      menu_id as menuId,
      parent_id as parentId,
      user_id as userId,
      title,
      content,
      view_count as viewCount,
      comment_count as commentCount,
      created_at as createdAt,
      updated_at as updatedAt
    from post
    where menu_id = 1
  </select>

</mapper>